# Implementation Plans

Generated by the `/improve` skill. Last full audit: **2026-07-04, commit `19a4695`**
(previous audit 2026-06-27 at `36699c4`). Executors: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Reconciliation 2026-07-04: the 12 plans completed since the first audit
(001–008, 010–012, 014) were verified DONE against the code and **deleted**.
The 4 remaining TODO plans had all drifted and were rewritten against `19a4695`
(013, 015, 016; 017 reversed direction — see its header). Plans 018–022 are new.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [018](018-ci-gates-cache-audit.md) | CI runs lint + typecheck, pnpm caching, picomatch advisory fixed | P1 | S | — | TODO |
| [013](013-fix-line-numbers.md) | Fix line numbers — SWC spans are 1-based UTF-8 byte offsets | P1 | M | — | TODO |
| [016](016-parsefile-error-handling-tests.md) | parseFile returns null on I/O error; pipeline records skipped files | P2 | S | 013 (ordering only) | TODO |
| [017](017-populate-component-files-field.md) | Populate ComponentUsage.files (was: remove it) | P2 | S | 013, 016 (ordering only) | DONE (2026-07-10, commit `5c6c333`, merged to `beta`) |
| [025](025-fix-swc-ast-argument-unwrapping.md) | Fix dead pattern detection: unwrap SWC `{spread, expression}` wrappers | P1 | S | — | TODO |
| [021](021-utils-unit-tests.md) | Unit tests: package-distribution, versus, pattern-counter, package-rules | P2 | M | — | DONE (2026-07-10, commit `4cd5716`, merged to `beta`) |
| [015](015-swc-parser-unit-tests.md) | SWC parser pattern + matcher unit tests (amended — see plan header) | P2 | M | — | DONE (2026-07-10, commit `4548d36`, merged to `beta`) |
| [019](019-parallel-file-parsing.md) | Parse files concurrently with async SWC parse() | P2 | M | 013, 016, 017 | TODO |
| [020](020-unify-cli-error-handling.md) | Unify scan/comply error handling + shared command setup | P3 | S | — | DONE (2026-07-10, commit `5200c5e`, merged to `beta`) |
| [022](022-small-cleanups-docs-sync.md) | Small cleanups: dead reports field, enricher lookup, config-v2, docs sync | P3 | S | — | TODO |
| [023](023-package-json-standards-rules.md) | package.json standards: forbid fields, value assertions, dot-paths | P3 | M | — | TODO |
| [024](024-codeowners-coverage-rule.md) | CODEOWNERS coverage rule (unowned scanned files fail comply) | P3 | M | 023 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED: <reason>` | `REJECTED: <reason>`

## Dependency notes

- **013 → 016 → 017 → 019** is ordering, not hard dependency, for the first
  three: all four touch `src/swc-parser/index.ts`, and 019's excerpts assume
  the other three have landed. Execute in that order to avoid conflicts.
- **019 requires 013/016/017 to be DONE** (its steps reference `lineOffsets`,
  the null contract, and the `generateReport(state, filePath)` signature).
- 015, 018, 020, 021, 022, 025 are independent of everything else and of each
  other. 025 was discovered during 015's execution (2026-07-10): the executor
  found that lazy/dynamic/HOC/memo/array detection never fires (SWC AST
  wrapper bug, reviewer-verified against the live AST). 015 was amended in
  place — its advanced/collections test files moved to 025 as that fix's
  regression suite. 013 touches the same `line:` expressions as 025; land
  order doesn't matter but rebase carefully if both are in flight.
- **024 requires 023** — both extend the `RuleViolation` union,
  `print-rules.ts`, and `evaluator.test.ts`; 024's excerpts assume 023 landed.
  024 also touches `pipeline.ts` (adds a parameter to `evaluateRules`), so if
  016/019 are queued, land them before 024 to avoid pipeline conflicts.
- Recommended batches: **(1)** 018 alone (makes CI catch mistakes in all later
  plans), **(2)** 013, 016, 017 in order, **(3)** 015, 020, 021, 022, 023 in
  any order, **(4)** 019, then 024 last.

## Direction options (maintainer decisions, not plans)

Grounded suggestions from the 2026-07-04 audit. The maintainer's own
`docs/TODO.md` is explicitly marked "do not think about at the moment", so
none of these were planned; they are recorded so the evidence isn't lost.

- **Yarn berry (v2+) lockfile support** — the adapter currently returns an
  empty version map for berry lockfiles (Plan 022 fixes the misleading label).
  Berry lockfiles are YAML and `js-yaml` is already a dependency, so support
  is one adapter method away. Effort ~M.
- **Baseline/diff mode for `comply`** — teams migrating incrementally can't
  gate on "no *new* violations"; the `versus` feature shows the project
  already cares about migration tracking. Effort ~M.
- **SARIF output format** — `comply` in CI + a `sarif` value for
  `output.format` would give GitHub annotations without glue scripts. Additive,
  effort ~L.
- **GitHub Action / programmatic integrations** — the new library entry point
  (`src/index.ts`) and typed JSON result exist but nothing consumes them yet.
  A published Action would be the cheapest proof of the API. Effort ~L.
- **Enable TypeScript strict mode** — `tsconfig.json` has `strict: false` and
  `noImplicitAny: false` while `CONTRIBUTING.md:8` claims "strict type
  checking enabled". Real value, but a multi-file fix effort (L) with the TS
  7 RC pin in play — needs a maintainer go-ahead.

## Unplanned findings (real, but below the leverage bar)

- **Conditional pattern ignores JSX-branch ternaries** (found 2026-07-10
  during Plan 015): `analyzeConditionalExpression`
  (`src/swc-parser/patterns/conditionals.ts`) only matches `Identifier`
  branches, so `cond ? <Button /> : null` never populates
  `usage.conditional` (the component still appears in `usage.jsx`). Distinct
  from the Plan 025 wrapper bug — ternary branches are not wrapped. Extending
  it to JSX branches is a design decision (overlap with jsx usage tracking),
  not a bug fix; characterized by a test in
  `tests/swc-parser/patterns/conditionals.test.ts`.
- **Pipeline/command orchestration unit tests** — `runPipeline`,
  `executeScan`, `executeComply` have e2e coverage only. Mock-heavy unit
  tests are M-effort with moderate value while e2e pins exit codes and JSON
  output; revisit after Plans 019/020 settle the code.
- **Docs generation flow unclear** — `docs-templates/` + `scripts/update-docs.js`
  exist but no npm script or CI step runs them; generated outputs are also
  committed. Needs a maintainer decision on the intended flow before a plan
  is worth writing (Plan 022 Step 5 works around it defensively).
- **`.npmrc` / registry auto-discovery for releaseAge** — private registries
  ARE supported today via `releaseAge.registry` + auth token config; what's
  missing is auto-discovery from `.npmrc`. Nice-to-have, M effort.

## Findings considered and rejected

Rejected in the 2026-07-04 audit pass:

- **"Parallelize with Promise.all around parseSync"** (perf subagent's fix
  sketch): wrong — parseSync is synchronous; Promise.all changes nothing. The
  correct fix (async `parse()`) is Plan 019.
- **`console.error(error)` as a security finding**: overstated — a local CLI
  printing its own stack to its own user is not an exposure. Kept as a UX/
  consistency cleanup in Plan 020.
- **Configurable enricher concurrency (`CONCURRENCY = 8`)**: YAGNI until a
  user asks; revisit if registry throttling reports appear.
- **Env-var parsing style in enricher (TTL ternary)**: code is correct;
  pure style preference. Not worth a plan.
- **Type-only tests for `src/index.ts` exports**: `tsc --noEmit` (CI-gated by
  Plan 018) already fails on broken type exports; a tsd-style suite adds
  little.
- **"releaseAge unusable for private registries"**: partially false —
  `releaseAge.registry` and `authToken`/`HERMEX_REGISTRY_AUTH_TOKEN` cover
  it; only `.npmrc` auto-discovery is missing (see unplanned findings).
- **enforceOnBump graduated severities**: thresholds are already per-bump
  (patch/minor/major each have one); a second per-bump severity axis is
  speculative until requested.

Carried over from the 2026-06-27 audit (still settled — do not re-audit):

- **SEC-01/02** (dynamic config import / path traversal in `--config`): by
  design — standard vite/eslint config pattern; user runs own code locally.
- **SEC-05** (js-yaml `load()` unsafe): false positive — v4+ safe by default.
- **SEC-04** (glob/micromatch ReDoS patterns in *our* usage): low confidence.
  (Distinct from the picomatch dependency advisory fixed in Plan 018.)
- **DEP-05** (chalk/ora/cli-table3 in `dependencies`): by design — runtime
  deps of a CLI.
- **PERF-05** (visitChildren `for...in`): standard visitor pattern, by design.
- **BUG-07** (JSX double processing): guarded by the Map; no impact.
- **LOGIC-04** (null concatenation in extractAssignmentInfo): harmless.
- **LOGIC-02** (jsxUsage stores first occurrence per component): by design.
- **allow_files semantics**: resolved 2026-07-03 (issues #14/#17/#18) —
  `allow_files`/`forbid_files` removed; `require_files` + `detect_files` are
  the two file-rule types.
- **engine-version prerelease edge** (`semver.minVersion(">=18.0.0-rc.1")`):
  known, low priority.
- **pnpm regex misses workspace packages**: known edge case.
- **isFromLibrary substring matching**: low confidence; depends on user input.
- **format-utils/chart-renderer unit tests**: low value for terminal display
  code.
- **Plan 009 (TypeScript RC pin)**: removed by maintainer decision 2026-07-03
  — the `typescript@7.0.1-rc` pin is intentional.
