# Implementation Plans

Generated by the `/improve` skill. Last full audit: **2026-07-24, commit
`a3b8f02`** (previous full audit 2026-07-04 at `19a4695`; reconciliation/
targeted passes through 2026-07-10 at `b8a6478`). Executors: read the plan
fully before starting, honor its STOP conditions, and update your row when
done.

## Small-tasks / code-quality pass 2026-07-25 (targeted)

A targeted pass at `ac69a10` (release `2.5.1`) scoped to **small,
maintainability-focused** findings only — not a full re-audit. Read the core
modules (lock-parser adapters, commands, config, rules, npm-registry
enricher, aggregator, format/severity utils, npm-registry client/cache,
chart-renderer, and the full print-* family) directly. The codebase is clean
and already heavily audited; the pass surfaced **two** new plan-worthy DRY
refactors plus two trivial items recorded below (not worth their own plan
files). (047 was found after an initial "one plan" answer was correctly
pushed back on — the print-* family had only been skimmed; reading it
directly surfaced the repeated table style.)

- **New plan 046** — `PnpmLockfileAdapter.parse()` duplicates its
  `dependencies`/`devDependencies` importer-extraction block
  (`src/lock-parser/patterns/pnpm.ts`, two ~13-line near-identical loops).
  Extract one helper; fully covered by existing tests. S/LOW.
- **New plan 047** — four `print-*` modules (`print-summary`, `print-packages`,
  `print-components`, `print-patterns`) each construct a `cli-table3` table
  with the identical `style: { head: ['cyan'], border: ['gray'] }` literal.
  Extract a `createReportTable(head)` factory; byte-identical output, covered
  by `tests/utils/print-utils.test.ts` + e2e. S/LOW.

Nothing else new: the enricher `findIndex`, dead `AggregatedReport.reports`
field, and yarn `supportedVersions` label are all already covered by
[plan 022](022-small-cleanups-docs-sync.md); the `parseFile | null` dead type
by [plan 032](032-remove-dead-parsefile-null-type.md). See "Findings
considered and rejected" for the two trivial items and the import-style note.

## Reconciliation 2026-07-24 (this session)

Local `main` was 2 commits behind `origin/main` at session start
(`c3d96e8` → `a3b8f02`, fast-forwarded cleanly — `09f46ac` relaxed
`engines.node`, `a3b8f02` is the resulting `2.4.1` release commit). A fresh
standard-effort audit ran across all 9 categories via 4 parallel subagents
plus direct verification, deliberately **not** biased by the 3 open plans'
own claims — each was independently re-derived, then reconciled against
what the fresh audit actually found:

- **013** (line numbers) — **reconfirmed unchanged**. All 16 `span?.start`
  sites are at the exact same lines as `19a4695`. Rewritten in place with a
  refreshed commit stamp and two small excerpt corrections (`generateReport`
  now takes a `filePath` parameter, unrelated to the bug itself).
- **016** (parseFile null handling) — **rejected**. Its premise (unreadable
  files could be silently dropped) doesn't hold: `pipeline.ts`'s existing
  `try/catch` already catches `readFileSync` failures and records them with
  their real error message. `parseFile` can never actually return `null`.
  Deleted; replaced by **032**, which does the opposite — tightens the
  return type to non-nullable and removes the dead check, rather than
  implementing a null path that would have been a message-quality
  regression.
- **022** (4 bundled cleanups) — **reconfirmed unchanged**. All 4
  sub-findings independently re-verified. One line number drifted (the
  enricher's `findIndex` moved from `196-202` to `258` as the function grew
  unrelated env-var handling) — excerpts refreshed.

The audit surfaced 14 new findings (table below) plus 1 new direction idea
and reconfirmed the 5 previously-recorded direction options. At the user's
explicit request, **all of them** were turned into plans this session —
17 plan files total (2 refreshed, 15 new), covering everything found. This
is a larger-than-default batch; see "Execution order & status" for the full
list and "Dependency notes" for sequencing.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [013](013-fix-line-numbers.md) | Fix line numbers — SWC span.start is a byte offset, not a line number | P1 | M | — | TODO |
| [022](022-small-cleanups-docs-sync.md) | Small cleanups: dead reports field, enricher lookup, config-v2, docs sync | P3 | S | — | TODO |
| [032](032-remove-dead-parsefile-null-type.md) | Remove parseFile's dead `\| null` return type | P2 | S | 013 (ordering only) | TODO |
| [042](042-yarn-berry-support-design-spike.md) | Real Yarn Berry (v2+) lockfile parsing (spike) | P3 | M | 022 (soft, done — see history), 037 (soft, done — see history) | TODO |
| [046](046-dedup-pnpm-importer-extraction.md) | Deduplicate the pnpm importer dependency/devDependency extraction in `parse()` | P3 | S | — | TODO |
| [047](047-shared-cli-table-factory.md) | Extract a shared cli-table3 factory for the repeated table style (4 print modules) | P3 | S | — | TODO |
| [040](040-sarif-output-design-spike.md) | SARIF output format for comply (spike) | P3 | L | — | TODO |
| [041](041-github-action-integration-design-spike.md) | Programmatic API surface for GitHub Action integration (spike) | P3 | L | — | TODO |
| [043](043-comply-baseline-diff-design-spike.md) | Baseline/diff mode for comply (spike) | P3 | L | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED: <reason>` | `REJECTED: <reason>`

10 plans from this table (030, 031, 033–039, 044) were executed this
session — see "Done this session (2026-07-24)" below for the full list,
PR links, and what happened. Their plan files have been deleted per the
same convention used for the 2026-07-10 batch (executed work is tracked by
its PR from here on, not a plan file).

## Dependency notes

- **013 → 032** is ordering, not a hard dependency: both touch
  `src/swc-parser/index.ts`. Execute in that order to avoid conflicts.
- **022 → 042** (soft, 022 side already done): `src/lock-parser/patterns/yarn.ts`'s
  `supportedVersions` line is now `['v1']` (honest about current v1-only
  support, landed via 022 — [PR #46](https://github.com/Gallevy/hermex/pull/46)
  incidentally, see history). 042's spike, if it proves Berry parsing works,
  should set it back to `['v1', 'v2+']`. 042's own drift check will catch
  the current value regardless.
- **037 → 042** (soft, 037 side already done): shared lockfile-adapter
  helpers (`readAndParseLockfile`, `toSortedMultiVersionMap`) landed via 037
  ([PR #46](https://github.com/Gallevy/hermex/pull/46)) — 042 should build
  a Berry adapter on top of these rather than duplicating boilerplate.
- All other plans are independent and can run in any order.
- **Lesson from this session, for future ordering advice**: this index
  originally recommended running 039 before 038 (038's optional
  optimization would then have a stable `findOwningEntry` surface to
  target). The user's execution order ran 038 before 039 instead, and 039
  merged first — both branches had independently modified
  `src/rules/codeowners.ts`'s `fileIsOwned`, producing a real, unresolvable
  git conflict on 038's PR. It was resolved by closing that PR, rewriting
  038 against what 039 actually shipped (`findOwningEntry`), and
  re-executing — see [PR #49](https://github.com/Gallevy/hermex/pull/49).
  No data was lost, but it cost an extra round-trip. When two plans in the
  dependency notes have a "run X before Y" recommendation because they
  touch the same function, treat it as load-bearing, not a suggestion.

## Direction options — status

The 2026-07-04 audit recorded 5 direction options as maintainer-optional,
not planned. This session's user explicitly requested **all** of them be
turned into design/spike plans — they remain **maintainer-optional
features**, not bugs: a spike plan produces a design + prototype for
review, not a commitment to ship.

- **Yarn berry (v2+) lockfile support** → [plan 042](042-yarn-berry-support-design-spike.md) (spike) — not yet executed.
- **Baseline/diff mode for `comply`** → [plan 043](043-comply-baseline-diff-design-spike.md) (spike) — not yet executed.
- **SARIF output format** → [plan 040](040-sarif-output-design-spike.md) (spike) — not yet executed.
- **GitHub Action / programmatic integrations** → [plan 041](041-github-action-integration-design-spike.md) (spike) — not yet executed. Note: this spike's "Current state" section corrects the prior framing; the actual gap is that `src/index.ts` exports no runnable scan function at all yet, not just "nothing consumes it."
- **Enable TypeScript strict mode** → **fully shipped**, in two parts:
  measurement spike [PR #50](https://github.com/Gallevy/hermex/pull/50)
  (report at `docs/design/typescript-strict-mode-assessment.md`), then the
  follow-up implementation [PR #51](https://github.com/Gallevy/hermex/pull/51)
  (plan 045, requested immediately after the spike landed). The spike's
  headline finding held up under re-verification: the codebase was
  essentially already strict-clean. The 4 `noImplicitAny` sites the spike
  found in isolation turned out to be non-issues once `strictNullChecks`
  was also on (better inference resolved them for free) — the real fix
  surface was one ambient type declaration for `@yarnpkg/lockfile`, not
  code changes. `tsconfig.json` now has `strict: true` plus all 4
  independent flags; `CONTRIBUTING.md`'s strictness claim is true again.

Executed this session as a direct implementation plan (not a spike — the
extension point and data flow were fully confirmed by reading the code,
not inferred):
- **CODEOWNERS required-owner option** → [PR #48](https://github.com/Gallevy/hermex/pull/48).

## Findings considered and rejected

Considered in the 2026-07-25 small-tasks pass (real, but not worth a plan
file — noted so they aren't re-surfaced as "missing plans"):

- **JSDoc typo in `src/utils/file-utils.ts:7`** ("Glob pattenrs to ignore" →
  "patterns"). A one-word docs fix; genuinely below the bar for a handoff
  plan. Anyone editing `file-utils.ts` should fix it in passing.
- **`readFile` export in `src/utils/file-utils.ts:28` is only consumed by its
  own test** (`tests/utils/file-utils.test.ts`), not by `src/`. Borderline
  dead code, but it's a small, reasonable public utility and removing it also
  deletes its test — net-negative churn for no real gain. Left as-is.
- **`node:` import-prefix inconsistency** (5 files use `from 'node:fs'`/
  `'node:path'`, 7 use the bare `'fs'`/`'path'`). Normalizing repo-wide is
  cross-cutting churn touching a dozen files for zero behavior change — better
  handled by an oxlint rule (`unicorn/prefer-node-protocol` or equivalent) if
  the maintainer wants it enforced, than by a manual find-replace plan. Not
  planned.
- **`RuleViolation.type` omits `forbid_packages`** (`src/rules/shared.ts:6`):
  **not a bug** — `forbid_packages` violations use the separate
  `BannedPackageViolation` type via `detectBannedPackages`
  (`src/utils/package-rules.ts`), a deliberately distinct code path. The union
  correctly lists only the types produced by the `evaluateRules` path.

Rejected in the 2026-07-24 audit pass:

- **Plan 016's premise ("parseFile could silently drop unreadable files")**:
  wrong — `pipeline.ts`'s `try/catch` already catches `readFileSync`
  failures and records them with their real message; `parseFile` can never
  actually return `null` today. Plan 016 deleted; replaced by
  [032](032-remove-dead-parsefile-null-type.md), which tightens the type
  instead of implementing a null path (implementing it as 016 proposed
  would have replaced specific error messages with a generic
  `'Could not read file'`, a regression).
- A correctness subagent's initial framing of the GitHub Action direction
  item ("library entry point and typed result exist but nothing consumes
  them") was corrected during planning: `src/index.ts` doesn't actually
  export a runnable scan function at all — see
  [plan 041](041-github-action-integration-design-spike.md)'s "Current
  state" for the verified gap.

Rejected in the 2026-07-04 audit pass:

- **"Parallelize with Promise.all around parseSync"** (perf subagent's fix
  sketch): wrong — parseSync is synchronous; Promise.all changes nothing. The
  correct fix (async `parse()`) is Plan 019.
- **`console.error(error)` as a security finding**: overstated — a local CLI
  printing its own stack to its own user is not an exposure. Kept as a UX/
  consistency cleanup in Plan 020.
- **Configurable enricher concurrency (`CONCURRENCY = 8`)**: YAGNI until a
  user asks; revisit if registry throttling reports appear.
- **Env-var parsing style in enricher (TTL ternary)**: code is correct;
  pure style preference. Not worth a plan.
- **Type-only tests for `src/index.ts` exports**: `tsc --noEmit` (CI-gated by
  Plan 018) already fails on broken type exports; a tsd-style suite adds
  little.
- **"releaseAge unusable for private registries"**: partially false —
  `releaseAge.registry` and `authToken`/`HERMEX_REGISTRY_AUTH_TOKEN` cover
  it; only `.npmrc` auto-discovery is missing (see unplanned findings).
- **enforceOnBump graduated severities**: thresholds are already per-bump
  (patch/minor/major each have one); a second per-bump severity axis is
  speculative until requested.

Carried over from the 2026-06-27 audit (still settled — do not re-audit):

- **SEC-01/02** (dynamic config import / path traversal in `--config`): by
  design — standard vite/eslint config pattern; user runs own code locally.
  Reconfirmed 2026-07-24.
- **SEC-05** (js-yaml `load()` unsafe): false positive — v4+ safe by default.
  Reconfirmed 2026-07-24.
- **SEC-04** (glob/micromatch ReDoS patterns in *our* usage): low confidence.
  Distinct from the picomatch dependency advisory fixed in Plan 018, and
  also distinct from the brace-expansion CVE found 2026-07-24
  ([plan 031](https://github.com/Gallevy/hermex/pull/41), executed and
  merged — plan file deleted) — that one is a real,
  HIGH-confidence, HIGH-severity dependency advisory, not the same as this
  low-confidence "our own glob usage" item.
- **DEP-05** (chalk/ora/cli-table3 in `dependencies`): by design — runtime
  deps of a CLI.
- **PERF-05** (visitChildren `for...in`): standard visitor pattern, by design.
  Reconfirmed 2026-07-24 — no perf findings in the visitor/matchers/
  jsx-helpers modules this pass either.
- **BUG-07** (JSX double processing): guarded by the Map; no impact.
- **LOGIC-04** (null concatenation in extractAssignmentInfo): harmless.
- **LOGIC-02** (jsxUsage stores first occurrence per component): by design.
- **allow_files semantics**: resolved 2026-07-03 (issues #14/#17/#18) —
  `allow_files`/`forbid_files` removed; `require_files` + `detect_files` are
  the two file-rule types.
- **engine-version prerelease edge** (`semver.minVersion(">=18.0.0-rc.1")`):
  known, low priority.
- **pnpm regex misses workspace packages**: known edge case.
- **isFromLibrary substring matching**: low confidence; depends on user input.
- **format-utils/chart-renderer unit tests**: low value for terminal display
  code.
- **Plan 009 (TypeScript RC pin)**: removed by maintainer decision 2026-07-03
  — the `typescript@7.0.1-rc` pin is intentional. Still intentional as of
  `typescript@7.0.2`, 2026-07-24 — plan 044's strict-mode spike explicitly
  does not revisit this pin, only measures strict-mode's cost against it.

## Unplanned findings (real, but below the leverage bar)

Carried from the 2026-07-04 audit, reconfirmed 2026-07-24 unless noted:

- **Conditional pattern ignores JSX-branch ternaries**: `analyzeConditionalExpression`
  (`src/swc-parser/patterns/conditionals.ts`) only matches `Identifier`
  branches, so `cond ? <Button /> : null` never populates
  `usage.conditional`. Extending it to JSX branches is a design decision
  (overlap with jsx usage tracking), not a bug fix.
- **Pipeline/command orchestration unit tests** — `runPipeline`,
  `executeScan`, `executeComply` have e2e coverage only. Still M-effort/
  moderate value, still deprioritized — not picked up in this session's
  "everything" instruction because it wasn't re-surfaced as a table finding
  (the fresh audit's test-coverage subagent explicitly re-checked and did
  not re-flag it, consistent with the prior deprioritization).
- **Docs generation flow unclear** — `docs-templates/` + `scripts/update-docs.js`
  exist but no npm script or CI step runs them; generated outputs are also
  committed. Still needs a maintainer decision.
- **`.npmrc` / registry auto-discovery for releaseAge** — still missing;
  nice-to-have, M effort.

## Done this session (2026-07-24, plan files deleted)

Executed via `/improve execute`, one plan at a time from a fresh `main`,
each reviewed (re-ran done criteria, checked scope, read the full diff)
before push + PR. PR merge status as of this writing; check the links for
current state.

| Plan | Title | PR | Merge status |
|------|-------|----|----|
| 030 | README accuracy pass — CLI usage, Node version, tech stack | [#40](https://github.com/Gallevy/hermex/pull/40) | merged |
| 031 | Patch brace-expansion ReDoS (CVE-2026-13149) — no override needed, dropped the pre-existing picomatch override too | [#41](https://github.com/Gallevy/hermex/pull/41) | merged |
| 033 | Test coverage for spread-prop and complex-prop detection | [#42](https://github.com/Gallevy/hermex/pull/42) | merged |
| 034 | Stop re-sorting package list on every import-path resolution (O(P) instead of O(P log P)) | [#43](https://github.com/Gallevy/hermex/pull/43) | open |
| 035 | CONTRIBUTING.md accuracy — real commands, correct structure, honest strictness claim | [#44](https://github.com/Gallevy/hermex/pull/44) | merged |
| 036 | Document the codeowners and forbid_package_fields config rules | [#45](https://github.com/Gallevy/hermex/pull/45) | merged |
| 037 | Extract shared read/parse/error-handling helpers across lockfile adapters | [#46](https://github.com/Gallevy/hermex/pull/46) | merged |
| 038 | Precompile CODEOWNERS matchers (31.6x speedup at 500×5,000 scale) | [#49](https://github.com/Gallevy/hermex/pull/49) (supersedes closed #47) | open |
| 039 | Add requiredOwners option to the codeowners rule | [#48](https://github.com/Gallevy/hermex/pull/48) | merged |
| 044 | Assess TypeScript strict-mode migration blast radius (measurement spike, no `tsconfig.json` change) | [#50](https://github.com/Gallevy/hermex/pull/50) | open |
| 045 | Enable TypeScript strict mode (all 12 flags) — follow-up to 044, requested by the maintainer right after the spike report landed | [#51](https://github.com/Gallevy/hermex/pull/51) | open |

Notes:
- **045** re-verified plan 044's numbers from scratch before writing (twice
  — once before drafting, once as the plan's own Step 1) rather than
  trusting the spike report at face value, and found the real fix surface
  was smaller than the spike's per-flag numbers implied: the 4
  `noImplicitAny` sites the spike found in isolation are not errors under
  the actual combined `strict: true` configuration (better inference from
  `strictNullChecks` resolves them). Only one real gap existed — a missing
  type declaration for `@yarnpkg/lockfile` — fixed with a small ambient
  `.d.ts` file. `src/config/schema.ts` and `src/rules/script-rules.ts`,
  which the spike's isolated numbers might suggest needed fixing, were
  correctly left untouched.
- **045**'s `pnpm run test:ci` run also hit the same pre-existing
  `tests/e2e/cli.test.ts` hook-timeout flake as 038/#49 (below) — the
  executor stopped on it per the plan's STOP condition, investigated with
  `git stash` before reporting, correctly diagnosed it as unrelated and
  pre-existing, and was then instructed to proceed after the advisor
  independently re-confirmed the same diagnosis a third time. Worth fixing
  (raise the hook's timeout, or run e2e in isolation in CI) as its own
  small plan if this keeps costing review round-trips.
- **045 correction (commit `ef3b939` on the same PR #51)**: the plan's
  fix for the `@yarnpkg/lockfile` missing-types gap originally used a
  hand-written `declare module` ambient declaration, based on an
  unverified claim (carried over from plan 044's report without being
  independently re-checked) that no `@types/yarnpkg__lockfile` package
  exists on npm. It does — `@types/yarnpkg__lockfile@1.1.9`
  (DefinitelyTyped), API-compatible with this codebase's usage. The
  advisor caught this after the user asked why a hand-written declaration
  was used instead of a real types package, verified the package actually
  exists and matches, and pushed a follow-up commit to the same PR
  swapping the hand-written file for the real devDependency. **Lesson**:
  a subagent's "verified: X → 404" claim in a report is not itself
  verification — it must be independently re-run before being repeated in
  a plan or acted on, per the skill's own rule that excerpts must come
  from the advisor's own reads.
- **032** (parseFile dead null type) was **not** executed this session —
  it's still a live plan file, TODO in the table above. Don't confuse it
  with 031; they were adjacent in the execution list but 032 wasn't
  requested.
- **038/039 conflict**: 038's first attempt ([PR #47](https://github.com/Gallevy/hermex/pull/47),
  closed) was cut before 039 merged and refactored `fileIsOwned` into
  `findOwningEntry`; both branches touched the same function independently
  and 038's PR became unmergeable. Rewrote 038 against current `main`
  (targeting `findOwningEntry` instead) and re-executed — see PR #49. The
  fix ended up strictly better than the original: since 039's
  `requiredOwners` check also calls `findOwningEntry` directly, the
  precompiled-matcher optimization now speeds up both the ownership check
  and the required-owner check, not just the one path originally measured.
- **031**'s investigation (prompted by an explicit request to avoid
  `pnpm.overrides` where possible) found the brace-expansion fix needed no
  override at all — `minimatch@10.2.5`'s own declared range already
  permitted the patched version, the lockfile just hadn't been refreshed —
  and, as a bonus, found the *pre-existing* `micromatch>picomatch` override
  (added in commit `cee1c81` after two prior direct-update attempts had
  side effects) is now redundant too. Both removed; `package.json`'s
  `pnpm.overrides` block no longer exists.
- **034** and **038/#49** and **044/#50** are still open PRs as of this
  writing — nothing further needed from `/improve`, they're awaiting
  merge.

## Done this session (2026-07-10, all merged to `beta`, plan files deleted)

| Plan | Title | Commit |
|------|-------|--------|
| 015 | SWC parser pattern + matcher unit tests (amended — advanced/collections split to 025) | `4548d36` |
| 017 | Populate ComponentUsage.files | `5c6c333` |
| 018 | CI lint + typecheck gates, pnpm caching, picomatch advisory fixed (via `pnpm.overrides` fallback) | `cee1c81` |
| 020 | Unify scan/comply error handling + shared command setup | `5200c5e` |
| 021 | Unit tests: package-distribution, versus, pattern-counter, package-rules | `4cd5716` |
| 023 | package.json standards: forbid fields, value assertions, dot-paths | `7a35675` |
| 024 | CODEOWNERS coverage rule (unowned scanned files fail comply) | `d605ed9` |
| 025 | Fix dead pattern detection: unwrap SWC `{spread, expression}` wrappers | `d3e2656` |
| 026 | Skip `.d.ts`/`.d.mts`/`.d.cts` files instead of parsing them (#22) | `ab68b88` |
| 027 | Compute `minCompliantVersion` for every semver bump tier, not just patch (#21) | `7fbbeaa` |
| 028 | Route parse-error diagnostics to stderr under `output.format: json` (#23), plus an oxfmt follow-up fix | `19f988d`, `9c9835c` |
| 029 | Add `defineConfig` export documented in examples but never implemented (#19) — merged locally, not pushed | `cb1f149` |

Notes on 026–028, kept for history since the plan files are gone:
- **026 (#22)** rejected the issue's own suggested fix (extend
  `swcOptionsForFile` to pass through `.d.ts`-aware SWC options) — verified
  directly against the installed `@swc/core@1.15.43` that no such option
  exists, even via the filename-aware `parseFileSync`. Shipped fix instead
  skips declaration files before they reach the parser.
- **027 (#21)** was generalized beyond the issue's literal ask: rather than a
  second hardcoded `bump === 'major'` branch, the fix is a single
  `thresholds[bump]` lookup working for patch/minor/major uniformly — this
  also fixed an un-reported identical gap on the `minor` tier.
- **028 (#23)**'s executor caught that none of the three plans listed
  `pnpm run format:ci` as a verification command even though it's a real CI
  gate (`.github/workflows/*.yaml` both run it) — a one-line oxfmt violation
  in `print-errors.ts` slipped through all three plans' own verification
  steps and was only caught in the tech-lead review pass before merge, fixed
  in a follow-up commit. **Every plan written since (013, 022, and all of
  030-044) includes `pnpm run format:ci`/`pnpm run lint` in its verification
  commands.**
- **029 (#19)** turned out to be a "two-thirds already fixed" issue: the
  `.d.ts`-publishing gap and `HermexScanResult` field-coverage gap it
  reported were both already resolved on `beta` by the time it was picked
  up. The only real, still-reproducing gap was `defineConfig` — fixed as a
  one-line identity function, now `src/index.ts`'s only runtime export
  (confirmed still true as of `a3b8f02`, and directly relevant to
  [plan 041](041-github-action-integration-design-spike.md)'s finding that
  it's the *only* runtime export).
