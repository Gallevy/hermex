<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-color`

The coloured path a developer actually sees in a terminal. Captured raw, so escape sequences are part of the diff.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/hermex.config.ts`](../hermex.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |
| **Environment** | `NO_COLOR` unset, `FORCE_COLOR=3` |
| **Colour** | captured raw, so escape sequences are part of the recorded output |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-color/`](../../tests/__output_baselines__/comply-color), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter comply-color
```

Defined in [`fixtures/cases.ts`](../cases.ts).
