<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-overrides`

Repo-scoped overrides re-scope severities: one rule downgraded to warn, one switched off and gone from the table.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/overrides.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/overrides.config.ts`](../configs/overrides.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-overrides/`](../../tests/__output_baselines__/comply-overrides), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, once a reviewer applies `baseline:approved` and the bot regenerates it — see `.github/workflows/baseline-approve.yaml`.

## Run it locally

```bash
pnpm run test:output -- --filter comply-overrides
```

Defined in [`fixtures/cases.ts`](../cases.ts).
