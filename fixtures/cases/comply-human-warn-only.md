<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-human-warn-only`

Warn and info findings are reported but do not fail the build — verdict wording plus exit 0.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/warn-only.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/warn-only.config.ts`](../configs/warn-only.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `0` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-human-warn-only/`](../../tests/__output_baselines__/comply-human-warn-only), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter comply-human-warn-only
```

Defined in [`fixtures/cases.ts`](../cases.ts).
