<!-- Generated by `pnpm run test:output`, every run. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-deprecated-packages`

Deprecation detection with release-age switched off entirely — the #107 case. Before, deprecation was a by-product of release-age enrichment, so this configuration found nothing at all and this run would have been silently compliant on that axis. Now the registry is consulted for deprecation alone: a no-deprecated-packages row appears in the Rules table carrying npm own notice, and at severity error it fails comply on its own. The only case where the registry is reached without release-age, which is exactly the path that did not exist before.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/deprecated-packages.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/deprecated-packages.config.ts`](../configs/deprecated-packages.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |
| **Registry** | offline, served from [`fixtures/registry/timelines.ts`](../registry/timelines.ts) — never the network |

## Recorded output

Nothing is committed for this case's output — every run compares this tree's real output against a fresh build of the target branch (`--against`, `main` by default). A change to hermex's output shows up as a diff in that run's output-review report, never as a file in this repo.

## Run it locally

```bash
pnpm run test:output -- --filter comply-deprecated-packages
```

Defined in [`fixtures/cases.ts`](../cases.ts).
