<!-- Generated by `pnpm run test:output`, every run. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-release-age-unscoped`

An empty enforceOn enforces nothing rather than everything: every installed package is still fetched and reported, every release-age row is advisory, and the exit code comes from rule violations alone. Includes moment — declared, installed, never imported — which was invisible to release age before #171.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/release-age-unscoped.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/release-age-unscoped.config.ts`](../configs/release-age-unscoped.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |
| **Registry** | offline, served from [`fixtures/registry/timelines.ts`](../registry/timelines.ts) — never the network |

## Recorded output

Nothing is committed for this case's output — every run compares this tree's real output against a fresh build of the target branch (`--against`, `main` by default). A change to hermex's output shows up as a diff in that run's output-review report, never as a file in this repo.

## Run it locally

```bash
pnpm run test:output -- --filter comply-release-age-unscoped
```

Defined in [`fixtures/cases.ts`](../cases.ts).
