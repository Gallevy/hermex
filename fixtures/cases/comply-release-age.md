<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-release-age`

The flagged-packages table, against a recorded registry: an overdue package with no in-window target (#26), one with a real target, and one merely coming due.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/release-age.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/release-age.config.ts`](../configs/release-age.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |
| **Registry** | offline, served from [`fixtures/registry/timelines.ts`](../registry/timelines.ts) — never the network |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-release-age/`](../../tests/__output_baselines__/comply-release-age), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter comply-release-age
```

Defined in [`fixtures/cases.ts`](../cases.ts).
