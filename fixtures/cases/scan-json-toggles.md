<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `scan-json-toggles`

What output.* toggles do to --format json: today, nothing (#91). The payload below is emitted with every section switched off, yet still carries packages, components, versus and ruleViolations in full. Pair it with scan-human-minimal to see the two formats diverge; when #91 lands, this baseline shrinking is the proof.

## What runs

| | |
| --- | --- |
| **Command** | `hermex scan --format json --config configs/minimal.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/minimal.config.ts`](../configs/minimal.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `0` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/scan-json-toggles/`](../../tests/__output_baselines__/scan-json-toggles), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter scan-json-toggles
```

Defined in [`fixtures/cases.ts`](../cases.ts).
