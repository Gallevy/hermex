<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-summary-file`

The markdown a consumer pastes into a PR comment or job summary — ANSI-free, rules + flagged packages + verdict.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --summary-file $OUT/summary.md` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/hermex.config.ts`](../hermex.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `1` |
| **Writes** | `summary.md` into a scratch directory outside the repo |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-summary-file/`](../../tests/__output_baselines__/comply-summary-file), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, once a reviewer applies `baseline:approved` and the bot regenerates it — see `.github/workflows/baseline-approve.yaml`.

## Run it locally

```bash
pnpm run test:output -- --filter comply-summary-file
```

Defined in [`fixtures/cases.ts`](../cases.ts).
