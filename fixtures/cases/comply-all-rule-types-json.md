<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-all-rule-types-json`

The machine-readable shape of every rule type: fieldPath and actualValue on package-field hits, installedRange/requiredRange on engine_version, matchedFiles on codeowners. Also where #95 is visible — the two codeowners entries are byte-identical apart from matchedFiles.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --format json` |
| **Working directory** | `fixtures/repos/all-rule-types` |
| **Config** | [`fixtures/repos/all-rule-types/hermex.config.ts`](../repos/all-rule-types/hermex.config.ts) |
| **Fixture** | [`fixtures/repos/all-rule-types`](../repos/all-rule-types) — [overview](../repos/all-rule-types/README.md) |
| **Asserted exit code** | `1` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-all-rule-types-json/`](../../tests/__output_baselines__/comply-all-rule-types-json), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter comply-all-rule-types-json
```

Defined in [`fixtures/cases.ts`](../cases.ts).
