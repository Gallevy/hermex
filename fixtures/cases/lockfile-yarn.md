<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `lockfile-yarn`

yarn.lock produces the same inventory as its siblings.

## What runs

| | |
| --- | --- |
| **Command** | `hermex scan --format json` |
| **Working directory** | `fixtures/repos/lockfile-yarn` |
| **Config** | _none — no `hermex.config.ts` in the working directory, so this runs on schema defaults_ |
| **Fixture** | [`fixtures/repos/lockfile-yarn`](../repos/lockfile-yarn) — [overview](../repos/lockfile-yarn/README.md) |
| **Asserted exit code** | `0` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/lockfile-yarn/`](../../tests/__output_baselines__/lockfile-yarn), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, once a reviewer applies `baseline:approved` and the bot regenerates it — see `.github/workflows/baseline-approve.yaml`.

## Run it locally

```bash
pnpm run test:output -- --filter lockfile-yarn
```

Defined in [`fixtures/cases.ts`](../cases.ts).
