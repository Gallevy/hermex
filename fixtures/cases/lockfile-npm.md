<!-- Generated by `pnpm run test:output`, every run. Edit the case in
     fixtures/cases.ts, not this file. -->

# `lockfile-npm`

package-lock.json produces the same inventory as its siblings.

## What runs

| | |
| --- | --- |
| **Command** | `hermex scan --format json` |
| **Working directory** | `fixtures/repos/lockfile-npm` |
| **Config** | _none — no `hermex.config.ts` in the working directory, so this runs on schema defaults_ |
| **Fixture** | [`fixtures/repos/lockfile-npm`](../repos/lockfile-npm) — [overview](../repos/lockfile-npm/README.md) |
| **Asserted exit code** | `0` |

## Recorded output

Nothing is committed for this case's output — every run compares this tree's real output against a fresh build of the target branch (`--against`, `main` by default). A change to hermex's output shows up as a diff in that run's output-review report, never as a file in this repo.

## Run it locally

```bash
pnpm run test:output -- --filter lockfile-npm
```

Defined in [`fixtures/cases.ts`](../cases.ts).
