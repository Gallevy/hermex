<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-exit-2`

A pipeline failure (nothing matched `includes`) exits 2, not 1 — a consumer must be able to tell "could not run" from "not compliant".

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config configs/no-files.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/no-files.config.ts`](../configs/no-files.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `2` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-exit-2/`](../../tests/__output_baselines__/comply-exit-2), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, once a reviewer applies `baseline:approved` and the bot regenerates it — see `.github/workflows/baseline-approve.yaml`.

## Run it locally

```bash
pnpm run test:output -- --filter comply-exit-2
```

Defined in [`fixtures/cases.ts`](../cases.ts).
