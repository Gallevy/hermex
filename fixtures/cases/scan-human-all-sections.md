<!-- Generated by `pnpm run test:output`, every run. Edit the case in
     fixtures/cases.ts, not this file. -->

# `scan-human-all-sections`

Every human section rendered at once, including details and patterns, which the default config leaves off.

## What runs

| | |
| --- | --- |
| **Command** | `hermex scan --config configs/all-sections.config.ts` |
| **Working directory** | `fixtures/` |
| **Config** | [`fixtures/configs/all-sections.config.ts`](../configs/all-sections.config.ts) |
| **Fixture** | [`fixtures`](..) — [overview](../README.md) |
| **Asserted exit code** | `0` |

## Recorded output

Nothing is committed for this case's output — every run compares this tree's real output against a fresh build of the target branch (`--against`, `main` by default). A change to hermex's output shows up as a diff in that run's output-review report, never as a file in this repo.

## Run it locally

```bash
pnpm run test:output -- --filter scan-human-all-sections
```

Defined in [`fixtures/cases.ts`](../cases.ts).
