<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `release-age-tree-scope`

scope: tree enforces every resolved copy, so the nested version becomes the mandatory failure and the reported installed version follows it.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply --config tree.config.ts` |
| **Working directory** | `fixtures/repos/version-conflict` |
| **Config** | [`fixtures/repos/version-conflict/tree.config.ts`](../repos/version-conflict/tree.config.ts) |
| **Fixture** | [`fixtures/repos/version-conflict`](../repos/version-conflict) — [overview](../repos/version-conflict/README.md) |
| **Asserted exit code** | `1` |
| **Registry** | offline, served from [`fixtures/registry/timelines.ts`](../registry/timelines.ts) — never the network |

## Recorded output

The committed baseline is [`tests/__output_baselines__/release-age-tree-scope/`](../../tests/__output_baselines__/release-age-tree-scope), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, in the same PR that causes it.

## Run it locally

```bash
pnpm run test:output -- --filter release-age-tree-scope
```

Defined in [`fixtures/cases.ts`](../cases.ts).
