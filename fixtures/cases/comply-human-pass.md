<!-- Generated by `pnpm run test:output -- --update`. Edit the case in
     fixtures/cases.ts, not this file. -->

# `comply-human-pass`

A repo that satisfies every rule: the clean verdict wording and exit 0.

## What runs

| | |
| --- | --- |
| **Command** | `hermex comply` |
| **Working directory** | `fixtures/repos/compliant` |
| **Config** | [`fixtures/repos/compliant/hermex.config.ts`](../repos/compliant/hermex.config.ts) |
| **Fixture** | [`fixtures/repos/compliant`](../repos/compliant) — [overview](../repos/compliant/README.md) |
| **Asserted exit code** | `0` |

## Recorded output

The committed baseline is [`tests/__output_baselines__/comply-human-pass/`](../../tests/__output_baselines__/comply-human-pass), which holds this case's stdout, stderr, exit code and any file it wrote. A change to hermex's output shows up as a diff there, once a reviewer applies `baseline:approved` and the bot regenerates it — see `.github/workflows/baseline-approve.yaml`.

## Run it locally

```bash
pnpm run test:output -- --filter comply-human-pass
```

Defined in [`fixtures/cases.ts`](../cases.ts).
