---

---

Deliberately unreleasable. The `v3Expected` marker lives entirely in the
output-review harness — `scripts/`, `fixtures/` and `tests/` — which is repo
tooling, not part of the published package. Nothing a consumer of hermex can
observe changes.
